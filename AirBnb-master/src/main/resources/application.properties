spring.application.name=AirBnb

# DB Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/airbnb_db
spring.datasource.username=root
spring.datasource.password=yourpassword
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.servlet.context-path=/api/v1
server.port=8080

# JWT Secret
jwt.secretKey=MySuperSecretKeyForJWT12345

# Frontend URL
frontend.url=http://localhost:3000

# Stripe Configuration (dummy for local)
stripe.secret.key=dummy_key_for_now
stripe.webhook.secret=dummy_webhook_key

# Swagger Configuration
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
